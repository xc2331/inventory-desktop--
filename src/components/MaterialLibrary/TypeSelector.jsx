/*
 * TypeSelector.jsx
 * Re-exports ViewSwitcher and StatsCards from the main MaterialLibrary module
 * for logical sub-component grouping (P-04).
 */
export { ViewSwitcher, StatsCards } from '../MaterialLibrary'